# TempMailClient

Client web per [Catchmail](https://catchmail.io) (API v1). Single-user, deploy su Vercel.

## Setup
```bash
cp .env.example .env.local   # imposta AUTH_EMAIL, AUTH_PASSWORD, SESSION_SECRET
npm install
npm run dev
```

## Deploy
Push su GitHub → import in Vercel → imposta le env vars.
